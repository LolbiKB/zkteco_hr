// Re-export shim: this primitive now lives in the shared Dewey design
// system. Keep importing from "@/components/ui/badge" — only the source moved.
export { Badge, badgeVariants } from "@lolbikb/dewey-ui"
