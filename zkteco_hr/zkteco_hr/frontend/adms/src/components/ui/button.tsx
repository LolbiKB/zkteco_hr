// Re-export shim: this primitive now lives in the shared Dewey design
// system. Keep importing from "@/components/ui/button" — only the source moved.
export { Button, buttonVariants } from "@lolbikb/dewey-ui"
