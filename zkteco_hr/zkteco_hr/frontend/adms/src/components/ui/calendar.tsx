// Re-export shim: this primitive now lives in the shared Dewey design
// system. Keep importing from "@/components/ui/calendar" — only the source moved.
export { Calendar, CalendarDayButton } from "@lolbikb/dewey-ui"
