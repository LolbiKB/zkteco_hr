// Re-export shim: this primitive now lives in the shared Dewey design
// system. Keep importing from "@/components/ui/popover" — only the source moved.
export {
  Popover,
  PopoverAnchor,
  PopoverContent,
  PopoverDescription,
  PopoverHeader,
  PopoverTitle,
  PopoverTrigger,
} from "@lolbikb/dewey-ui"
