// Re-export shim: this primitive now lives in the shared Dewey design
// system. Keep importing from "@/components/ui/separator" — only the source moved.
export { Separator } from "@lolbikb/dewey-ui"
