// Re-export shim: this primitive now lives in the shared Dewey design
// system. Keep importing from "@/components/ui/table" — only the source moved.
export {
  Table,
  TableHeader,
  TableBody,
  TableFooter,
  TableHead,
  TableRow,
  TableCell,
  TableCaption,
} from "@lolbikb/dewey-ui"
