// Re-export shim: this primitive now lives in the shared Dewey design
// system. Keep importing from "@/components/ui/tooltip" — only the source moved.
export {
  Tooltip,
  TooltipContent,
  TooltipProvider,
  TooltipTrigger,
} from "@lolbikb/dewey-ui"
