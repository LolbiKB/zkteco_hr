// Re-export shim: this primitive now lives in the shared Dewey design
// system. Keep importing from "@/components/ui/scroll-area" — only the source moved.
export {
  ScrollArea,
  ScrollBar,
} from "@lolbikb/dewey-ui"
